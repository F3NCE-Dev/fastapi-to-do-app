version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite+aiosqlite:///app.db
      - STATIC_DIR=static
      - REDIRECT_URI=http://localhost:5500
    volumes:
      - ./backend/static:/app/static

  frontend:
    build: ./frontend
    ports:
      - "5500:80"